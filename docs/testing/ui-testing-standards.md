# UI Validation Standard

> [!IMPORTANT]
> UI validation for OpenTrusty MUST be executed against the embedded UI served by the OpenTrusty binary.
> Running the UI in isolation (e.g. Vite dev server) is not considered a valid system test.
